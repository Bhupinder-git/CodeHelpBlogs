import "./Spinner.css";

const Spinner = () => {
  return (
    <div className="flex h-screen w-full items-center justify-center -mt-30">
      <div className="spinner" />
    </div>
  );
};

export default Spinner;
